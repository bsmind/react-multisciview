import { calDataStat, shortKey } from './addDataSamples';

export default (state, payload) => {
    // const itemList = payload;

    // sampleKindsToUpdate = {}
    // dataBySamplesToUpdate = {}

    // itemList.forEach( (item, index) => {
    //     sampleName = item['sample'];
    //     if (sampleName in state.dataBySamples) {

    //         // add item to the list of the sample group
    //         // also update minmax
    //         // also update attrKinds
    //     } else {
    //         // update sampleKinds
    //     }
    // });

    return state;
}